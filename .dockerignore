# Keep .dockerignore minimal and valid (no Dockerfile instructions here)

# Ignore node_modules and caches
node_modules/
frontend/node_modules/
backend/node_modules/
.pnpm/
.yarn/
.npm/

# Logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (exclude all except example)
.env
.env.*
!.env.example

# VCS
.git/

# IDE/editor files
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Test / coverage / reports
coverage/
*.lcov
playwright-report/
cypress/screenshots/
cypress/videos/

# Frontend build artifacts (we build inside container)
frontend/dist/
frontend/build/

# Public folder - include if you want to copy static assets from host; otherwise it's safe to ignore so builder creates it
# If you rely on host public/, remove the leading # to include it in context
# public/

# Keep docker config files in context
!.dockerignore
!Dockerfile
!docker-compose.yml

# Ensure essential files are included if present
!package*.json
!server.js
!backend/
!frontend/
!config/
!scripts/
!contracts/
!arielmatrix2.0/
